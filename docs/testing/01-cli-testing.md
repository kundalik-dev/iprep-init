# CLI Testing

## Issues 11-05-2026

- ✅ .env file is created along side of .iprep not inside .iprep
- ✅ banner size is small need bigger banner
- `database reachable` is showing in `red` color in verifying setup step which is not correct
- for first time setup when don call internally `iprep start` command so user did not need to run iprep start command himself instead server is started and open browser to connect with our frontend url either deployed one or localhost:5743 one
- ✅ We need validation on database folder is exists or not if someone removed it still our validation is only for iPrep folder is exists or not but not all dir inside it which created during onboard command
-
